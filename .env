# Prisma generates this file. The actual DATABASE_URL is in .env.local
# See .env.local for real values (not committed to git)
DATABASE_URL="postgresql://user:password@localhost:5432/sg_website?schema=public"
